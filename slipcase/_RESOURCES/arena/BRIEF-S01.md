# S01 — FIELD-ROUTING   (read slipcase/_PROMPTS/S01__FIELD-ROUTING.txt first)
"Show the builder the field of open ports, not the castle."

Your discipline: before each batch, run `field --k 8` (or `--level`, `--near`) and choose where
the next pieces go FROM THE FIELD, by relation, never by planning coordinates in your head.
The field ranks ragged tops first, then ports near your last piece. You may PASS a port (just
not use it) but say why in NOTES.md: keep open for the stair / roof studs are the roof / wrong colour.
Start: `node world.js S01 init`, then `tray`, then `field`.
Hypothesis you are testing: routing placement by the field yields bonded pieces (no floats, no
clashes) and a structural open share inside the kit band without any coordinates.
