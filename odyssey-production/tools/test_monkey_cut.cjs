const fs=require('fs'),assert=require('assert');const root='',cut=JSON.parse(fs.readFileSync(root+'monkey-cut.json'));let end=0;for(const [i,s]of cut.shots.entries()){assert.equal(s.start,end);assert(s.duration>0);for(const k of ['from_','to','target'])assert(s[k].length===3&&s[k].every(Number.isFinite));assert(s.action&&s.line);if(i>0&&s.line.includes(':'))assert(fs.statSync(root+'monkey-audio/'+i+'.wav').size>1000);end+=s.duration;}assert.equal(end,cut.duration);assert.equal(cut.shots.length,13);assert.equal(end,83);console.log('PASS Night Shift: continuous 83s timeline, 13 camera/action cues, recorded dialogue assets');
